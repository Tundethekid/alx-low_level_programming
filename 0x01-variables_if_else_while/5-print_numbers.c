#include <stdio.h>

/**
 * main - prints all single digits numbers of base 10
 * Return: Always 0
 */
int main(void)
{
	int lm;

	for (lm = 0; lm <= 9; lm++)
		printf("%d", num);

	printf("\n");
	return (0);
}
